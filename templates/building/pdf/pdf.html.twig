{% extends 'pdf_nomenclator.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div>
        <strong>Proyecto:</strong> {{ project.name }}
        {% if filter %}
            <br>
            <strong>Filtrado por:</strong> {{ filter }}
        {% endif %}
    </div>
    <br>
    <table>
        {% block columns %}{% endblock %}
        {% for building in paginator.getData() %}
            {% block data %}{% endblock %}
        {% else %}
            <tr>
                <td colspan="{{ colspan|default(8) }}">No se encontraron obras.</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}
