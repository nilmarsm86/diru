{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Nueva Planta{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Plantas') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Plantas', href: path('app_floor_index', {'building':building.id})},
            {label: 'Nueva'},
        ]) }}
    </div>

    <h2 class="mb-4">Agregar planta: {{ building.name }}</h2>

    {{ include('floor/_form.html.twig', {'showButton': true, title: title}) }}
{% endblock %}
