{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Nueva Conexión de red{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Conexiones de red', 'icon-park-outline:rectangular-circular-connection') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Conexiones de red', href: path('app_network_connection_index')},
            {label: 'Nueva'},
        ]) }}
    </div>

    <h2 class="mb-4">Agregar conexión de red</h2>

    {{ include('network_connection/_form.html.twig', {showButton: true, title: title}) }}
{% endblock %}
