{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: 'id="local_detail_' ~ local.id,
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: title,
} %}

    {% block header %}
{#        {{ include('partials/_card_header_options.html.twig', {#}
{#            goTo: 'app_local_new',#}
{#            label: 'Nuevo',#}
{#            title: 'Nuevo local',#}
{#            entity: local,#}
{#            state: 'detail',#}
{#            showButton: (showButton is defined and showButton == true) ? true : false#}
{#        }) }}#}
    {% endblock %}

    {% block body %}
        <div class="table-responsive table mt-2" role="grid" aria-describedby="dataTable_info">
            <table class="table table-hover">
                <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ local.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ local.name }}</td>
                </tr>
                <tr>
                    <th>Number</th>
                    <td>{{ local.number }}</td>
                </tr>
                <tr>
                    <th>Area</th>
                    <td>{{ local.area }}</td>
                </tr>
                <tr>
                    <th>Type</th>
                    <td>{{ local.type }}</td>
                </tr>
                <tr>
                    <th>Height</th>
                    <td>{{ local.height }}</td>
                </tr>
                <tr>
                    <th>TechnicalStatus</th>
                    <td>{{ local.technicalStatus }}</td>
                </tr>

                </tbody>
            </table>
        </div>
    {% endblock %}

    {% block footer %}
{#        {{ include('partials/_card_footer.html.twig', {#}
{#            list: {href: 'app_local_index', label: 'Listado', title:'Volver al listado'},#}
{#            edit: {href: 'app_local_edit', label: 'Editar', title:'Editar local'},#}
{#            delete: {href: 'app_local_delete', label: 'Eliminar', title:'Eliminar local'},#}
{#            id: local.id,#}
{#            message: 'Está seguro que desea eliminar el local?',#}
{#            state: 'detail',#}
{#            showButton: (showButton is defined and showButton == true) ? true : false#}
{#        }) }}#}
    {% endblock %}

{% endcomponent %}


