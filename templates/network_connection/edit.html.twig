{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Editar Conexión de red{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Conexiones de red') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Conexiones', href: path('app_network_connection_index')},
            {label: 'Editar'},
        ]) }}
    </div>

    <h2 class="mb-4">Editar conexiones de red</h2>

    {{ include('network_connection/_form.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
