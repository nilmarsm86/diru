{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: '',
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: title,
} %}
    {% block header %}
        {{ include('province/_header.html.twig', {'print_link':'app_province_amount_finance_report_print'}) }}
    {% endblock %}

    {% block body %}
        {% component 'Table:Table' with {
            paginator: paginator,
            columns: [null, 'V. Aprobado', 'V. Estimado', 'P. Estimado', 'P. Detallado', 'V. Real']
        } %}

            {% block tableHead %}
                <tr>
                    <th>Nombre</th>
                    <th>V. Aprobado ({{ currency }})</th>
                    <th>V. Estimado ({{ currency }})</th>
                    <th>P. Estimado ({{ currency }})</th>
                    <th>P. Detallado ({{ currency }})</th>
                    <th>V. Real ({{ currency }})</th>
                </tr>
            {% endblock %}

            {% block tableBody %}
                {% from 'macros/link.html.twig' import filterLink %}

                {% for province in paginator.getData() %}
                    <tr>
                        <td>{{ province.name }}</td>
                        <td>{{ money(province.approvedValue, currency) }}</td>
                        <td>{{ money(province.estimatedValue, currency) }}</td>
                        <td>{{ money(province.estimatedAdjustValue, currency) }}</td>
                        <td>{{ money(province.constructionAssembly, currency) }}</td>
                        <td>{{ money(province.constructionRealValue, currency) }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6">No se encontraron provincias.</td>
                    </tr>
                {% endfor %}
            {% endblock %}

        {% endcomponent %}

    {% endblock %}

{% endcomponent %}
