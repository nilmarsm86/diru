{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Nuevo concepto{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Nuevo concepto') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Nuevo concepto', href: path('app_separate_concept_index')},
            {label: 'Nuevo'},
        ]) }}
    </div>

    <h2 class="mb-4">Agregar concepto</h2>

    {{ include('separate_concept/_form.html.twig', {showButton: true, title: title}) }}
{% endblock %}
