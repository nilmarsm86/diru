{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Nuevo sub tipo{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Subtipos') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Subtipos', href: path('app_subsystem_sub_type_index')},
            {label: 'Nuevo'},
        ]) }}
    </div>

    <h2 class="mb-4">Agregar subtipo</h2>

    {{ include('subsystem_sub_type/_form.html.twig', {'showButton': true, title: title}) }}
{% endblock %}
