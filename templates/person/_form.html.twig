{% from 'macros/buttons.html.twig' import submitFormButton %}

{% form_theme form 'form/bootstrap_5_horizontal_layout_sm.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}

<div class="row mb-3">
    <div class="col-sm-10 offset-sm-2">
        {{ submitFormButton() }}
    </div>
</div>
{{ form_end(form) }}
