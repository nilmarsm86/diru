{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}


{% block title %}Personas naturales{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Clientes', 'fa:user') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Personas naturales'},
        ]) }}
    </div>

    <h2 class="mb-4">Gestión de personas naturales</h2>

    {% component ManagePage %}
        {% block list %}
            {{ include('individual_client/_list.html.twig') }}
        {% endblock %}
    {% endcomponent %}

{% endblock %}
