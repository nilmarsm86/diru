{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}

{% block title %}Estimado de proyecto y preparación técnica: {{ ptp_estimate.concept }}{% endblock %}
{% from 'macros/titles.html.twig' import h1 %}

{% block content %}
    <div class="row">
        {{ h1('Estimados de proyecto y preparación técnica') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Estimados de proyecto y preparación técnica', href: path('app_ptp_estimate_index')},
            {label: 'Detalle'},
        ]) }}
    </div>

    <h2 class="mb-4">Estimado de proyecto y preparación técnica: {{ ptp_estimate.concept }}</h2>

    {{ include('ptp_estimate/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
