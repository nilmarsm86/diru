<form method="post" action="{{ path('app_enterprise_client_delete', {'id': enterprise_client.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enterprise_client.id) }}">
    <button class="btn">Delete</button>
</form>
