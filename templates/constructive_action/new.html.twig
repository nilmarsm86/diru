{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Nueva acción constructiva{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Acciones constructivas') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Acciones constructivas', href: path('app_constructive_action_index')},
            {label: 'Nueva'},
        ]) }}
    </div>

    <h2 class="mb-4">Agregar acción constructiva</h2>

    {{ include('constructive_action/_form.html.twig', {showButton: true, title: title}) }}
{% endblock %}
