{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Conceptos{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Conceptos') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Conceptos'},
        ]) }}
    </div>

    <h2 class="mb-4">Gestión de conceptos</h2>

    {% component ManagePage %}
        {% block list %}
            {{ include('separate_concept/_list.html.twig') }}
        {% endblock %}
    {% endcomponent %}
{% endblock %}
