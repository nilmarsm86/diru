{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Ciudad: {{ city.name }}{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Ciudades', 'icon-park:local') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Ciudades', href: path('app_city_index')},
            {label: 'Detalle'},
        ]) }}
    </div>

    <h2 class="mb-4">Ciudad: {{ city.name }}</h2>

    {{ include('city/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
