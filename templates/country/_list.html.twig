{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: '',
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: 'Listado de paises',
} %}
    {% block header %}
        {% from 'macros/buttons.html.twig' import tableActionButton2,tableActionButton3 %}

        <div>
            {{ tableActionButton3(path('app_country_new'), 'bi:plus-lg', 'Nuevo', 'Nuevo país', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_country_new')})) }}
        </div>
    {% endblock %}

    {% block body %}
        {% component 'Table:Table' with {
            paginator: paginator,
        } %}

            {% block tableHead %}
                <tr>
                    <th>Nombre</th>
                    <th></th>
                </tr>
            {% endblock %}

            {% block tableBody %}
                {% from 'macros/buttons.html.twig' import tableActionButton, deleteFormButton, deleteFormButton2, tableActionButton2, tableActionButton3 %}

                {% for country in paginator.getData() %}
                    <tr {{ stimulusController('delete-form-container') }}>
                        <td>{{ country.name }}</td>
                        <td class="text-end">
                            {{ tableActionButton3(path('app_country_show', {'id': country.id}), 'bi:eye', '', 'Detalle del país', stimulusAction('twig/table/table', 'selectElement:prevent', null, {url:path('app_country_show', {'id': country.id})}), 'btn-link') }}
                            {{ tableActionButton3(path('app_country_edit', {'id': country.id}), 'fa:edit', '', 'Editar país', stimulusAction('twig/table/table', 'selectElement:prevent', null, {url:path('app_country_edit', {'id': country.id})}), 'btn-link') }}

                            {{ component('DeleteForm', {
                                path: path('app_country_delete', {'id': country.id}),
                                confirm: 'Está seguro que desea eliminar el país?',
                                token: 'delete' ~ country.id,
                                title: 'Eliminar país',
                                icon: 'bi:trash',
                                cssClass: 'btn-link text-danger'
                            }) }}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="2">No se encontraron paises.</td>
                    </tr>
                {% endfor %}
            {% endblock %}

        {% endcomponent %}

    {% endblock %}

{% endcomponent %}
