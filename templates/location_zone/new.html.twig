{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Nueva Zona{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Zones de ubicación') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Zonas de ubicación', href: path('app_location_zone_index')},
            {label: 'Nueva'},
        ]) }}
    </div>

    <h2 class="mb-4">Agregar zona de ubicación</h2>

    {{ include('location_zone/_form.html.twig', {showButton: true, title: title}) }}
{% endblock %}
