{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: 'id="just_value_estimate_detail_' ~ just_value_estimate.id,
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: title,
} %}

    {% block header %}
        {{ include('partials/_card_header_options.html.twig', {
            goTo: 'app_just_value_estimate_new',
            label: 'Nueva',
            title: 'Nueva estimación de urbanización',
            entity: just_value_estimate,
            state: 'detail',
            showButton: (showButton is defined and showButton == true) ? true : false
        }) }}
    {% endblock %}

    {% block body %}
        {{ include('estimate/_detail.html.twig', {estimate: just_value_estimate}) }}
    {% endblock %}

    {% block footer %}
        {{ include('partials/_card_footer.html.twig', {
            list: {href: 'app_just_value_estimate_index', label: 'Listado', title:'Volver al listado'},
            edit: {href: 'app_just_value_estimate_edit', label: 'Editar', title:'Editar estimado de urbanización'},
            delete: {href: 'app_just_value_estimate_delete', label: 'Eliminar', title:'Eliminar estimado de urbanización'},
            id: just_value_estimate.id,
            message: 'Está seguro que desea eliminar el estimado de urbanización?',
            state: 'detail',
            showButton: (showButton is defined and showButton == true) ? true : false
        }) }}
    {% endblock %}

{% endcomponent %}


