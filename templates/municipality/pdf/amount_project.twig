{% extends 'municipality/pdf/pdf_municipality.html.twig' %}

{% block columns %}
    <tr>
        {{ th(0, 'column', 'Nombre') }}
        {{ th(1, 'column', 'Provincia') }}
        {{ th(2, 'column', 'Proyectos') }}
        {{ th(3, 'column', 'Obras') }}
    </tr>
{% endblock %}

{% block data %}
    <tr>
        {{ td(0, 'column', municipality.name) }}
        {{ td(1, 'column', municipality.province) }}
        {{ td(2, 'column', municipality.projects) }}
        {{ td(3, 'column', municipality.buildings) }}
    </tr>
{% endblock %}
