{% extends 'base.html.twig' %}

{% block title %}LocationZone{% endblock %}

{% block body %}
    <h1>LocationZone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location_zone.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ location_zone.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_location_zone_index') }}">back to list</a>

    <a href="{{ path('app_location_zone_edit', {'id': location_zone.id}) }}">edit</a>

    {{ include('location_zone/_delete_form.html.twig') }}
{% endblock %}
