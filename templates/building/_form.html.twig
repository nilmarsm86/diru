{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: '',
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: title
} %}

    {% block header %}
        {{ include('building/_buttons_header.html.twig', {'building': building}) }}
    {% endblock %}

    {% block body %}
        {{ component('BuildingForm', {modal:null, bui: building, ajax: ajax, project: (project is not null) ? project.id : 0}) }}
    {% endblock %}

    {% block footer %}
{#        {% if building.project is not null %}#}
{#            {% set list = {href: 'app_project_edit', data:{id: building.project.id}, label: 'Proyecto', title:'Volver al proyecto', icon: 'bi:table'} %}#}
{#        {% else %}#}
{#            {% set list = {href: 'app_project_index', data:{}, label: 'Proyectos', title:'Listado de proyectos', icon: 'bi:table'} %}#}
{#        {% endif %}#}

{#        {{ include('partials/_card_footer.html.twig', {#}
{#            'list': list,#}
{#            delete: null,#}
{#            id: building.id,#}
{#            message: '',#}
{#            state: 'form',#}
{#            showButton: (showButton is defined and showButton == true) ? true : false,#}
{#            entity: building#}
{#        }) }}#}
    {% endblock %}

{% endcomponent %}

{{ include('partials/modal/_modal_load.html.twig') }}
