{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}

{% block title %}Conexión de red: {{ network_connection.name }}{% endblock %}
{% from 'macros/titles.html.twig' import h1 %}

{% block content %}
    <div class="row">
        {{ h1('Conexión de red', 'icon-park-outline:rectangular-circular-connection') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Conexiones', href: path('app_network_connection_index')},
            {label: 'Detalle'},
        ]) }}
    </div>

    <h2 class="mb-4">Conexión de red: {{ network_connection.name }}</h2>

    {{ include('network_connection/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
