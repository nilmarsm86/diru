{% if app.request.query.get('modal') %}
    {{ component('BuildingRevisionForm', {modal:app.request.query.get('modal'), br: building_revision, ajax: ajax, screen: app.request.query.get('screen'), building: building}) }}
{% else %}
    {% component 'Card:Card' with {
        cssClass: 'mb-4',
        extra: '',
        headerCssClass: 'd-flex justify-content-between align-items-center',
        title: title
    } %}

        {% block header %}
            {{ include('partials/_card_header_options.html.twig', {
                goTo: '',
                label: 'Nueva',
                title: 'Nueva revisión',
                entity: building_revision,
                state: 'form',
                showButton: (showButton is defined and showButton == true) ? true : false
            }) }}
        {% endblock %}

        {% block body %}
            {{ component('BuildingRevisionForm', {modal:null, br: building_revision, ajax: ajax, building: building}) }}
        {% endblock %}

        {% block footer %}
            {{ include('partials/_card_footer.html.twig', {
                list: {href: 'app_building_revision_index', label: 'Listado', title:'Volver al listado'},
                delete: {href: 'app_building_revision_delete', label: 'Eliminar', title:'Eliminar revisión'},
                id: building_revision.id,
                message: 'Está seguro que desea eliminar la revisión?',
                state: 'form',
                showButton: (showButton is defined and showButton == true) ? true : false,
                entity: building_revision
            }) }}
        {% endblock %}

    {% endcomponent %}
{% endif %}
