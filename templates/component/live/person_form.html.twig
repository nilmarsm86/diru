<div {{ attributes.defaults(stimulusController('live-form', {modal:modal})) }}>

    {{ form_start(form, {
        attr: {
            'data-action': 'live#action:prevent',
            'data-live-action-param': 'save'
        }
    }) }}

    {{ form_row(form.name) }}

    <div class="mb-0 row">
        <div class="col">
            {{ form_row(form.identificationNumber, {label_attr:{'class':'col-sm-4'}}) }}
        </div>
        <div class="col">
            {{ form_row(form.passport, {label_attr:{'class':'col-sm-4'}}) }}
        </div>
    </div>

    <div class="row mb-3">
        <div class="col-sm-12">
            {{ component('button:submit', {
                icon: (this.per.id is null) ? 'bi:plus-lg' : 'fa:edit',
                label: (this.per.id is null) ? 'Agregar' : 'Modificar',
                component: this
            }) }}
        </div>
    </div>
    {{ form_end(form) }}
</div>
