{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}

{% block title %}Concepto: {{ separate_concept.number }}{% endblock %}
{% from 'macros/titles.html.twig' import h1 %}

{% block content %}
    <div class="row">
        {{ h1('Conceptos') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Conceptos', href: path('app_separate_concept_index')},
            {label: 'Detalle'},
        ]) }}
    </div>

    <h2 class="mb-4">Concepto: {{ representative.number }}</h2>

    {{ include('separate_concept/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
