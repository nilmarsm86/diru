{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Municipios{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Municipios', 'stash:pin-place') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Municipios'},
        ]) }}
    </div>

    <h2 class="mb-4">Gestión de municipios</h2>

    {% component ManagePage %}
        {% block list %}
            {{ include('municipality/_list.html.twig') }}
        {% endblock %}
    {% endcomponent %}
{% endblock %}
