{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}


{% block title %}Clientes{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Clientes', 'fa:user') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Personas naturales'},
        ]) }}
    </div>

    <h2 class="mb-4">Personas naturales</h2>

    {{ include('partials/_index_container.html.twig', {
        list: 'individual_client/_list.html.twig',
        rest_data_action: 'page:onChangeState->content-loader#refreshContent'
    }) }}

{% endblock %}
