{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Tipos de proyectos{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Tipos de proyecto', 'oi:project') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Tipos'},
        ]) }}
    </div>

    <h2 class="mb-4">Gestión de tipos de proyectos</h2>

    {% component ManagePage %}
        {% block list %}
            {{ include('ite_project_type/_list.html.twig') }}
        {% endblock %}
    {% endcomponent %}
{% endblock %}
