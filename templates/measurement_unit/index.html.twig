{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Unidades de medida{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Unidades de medida', 'formkit:unit') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Unidades'},
        ]) }}
    </div>

    <h2 class="mb-4">Gestión de unidades de medida</h2>

    {% component ManagePage %}
        {% block list %}
            {{ include('measurement_unit/_list.html.twig') }}
        {% endblock %}
    {% endcomponent %}
{% endblock %}
