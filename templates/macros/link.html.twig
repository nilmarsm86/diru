{% macro activeLink(link) %}
    {% if link in app.request.attributes.get('_route') %}active{% endif %}
{% endmacro %}

{% macro filterLink(filter) %}
    {{ path(app.request.attributes.get('_route'), {...app.request.query.all, 'filter': filter}) }}
{% endmacro %}
