{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}

{% block title %}Zona de ubicación: {{ location_zone.name }}{% endblock %}
{% from 'macros/titles.html.twig' import h1 %}

{% block content %}
    <div class="row">
        {{ h1('Zona de ubicación', 'bi:pin-map') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Zona', href: path('app_location_zone_index')},
            {label: 'Detalle'},
        ]) }}
    </div>

    <h2 class="mb-4">Zona de ubicación: {{ location_zone.name }}</h2>

    {{ include('location_zone/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
