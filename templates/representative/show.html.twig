{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}

{% block title %}Representante: {{ representative.name }}{% endblock %}
{% from 'macros/titles.html.twig' import h1 %}

{% block content %}
    <div class="row">
        {{ h1('Representantes') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Representantes', href: path('app_representative_index')},
            {label: 'Detalle'},
        ]) }}
    </div>

    <h2 class="mb-4">Representante: {{ representative.name }}</h2>

    {{ include('representative/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
