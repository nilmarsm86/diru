{% extends 'base.html.twig' %}

{% block title %}ConstructiveSystem{% endblock %}

{% block body %}
    <h1>ConstructiveSystem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ constructive_system.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ constructive_system.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_constructive_system_index') }}">back to list</a>

    <a href="{{ path('app_constructive_system_edit', {'id': constructive_system.id}) }}">edit</a>

    {{ include('constructive_system/_delete_form.html.twig') }}
{% endblock %}
