{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: '',
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: title,
} %}
    {% block header %}
            {{ include('province/_header.html.twig', {'print_link':'app_province_amount_project_report_print'}) }}
    {% endblock %}

    {% block body %}
        {% component 'Table:Table' with {
            paginator: paginator,
            columns: [null, 'Proyectos', 'Obras']
        } %}

            {% block tableHead %}
                <tr>
                    <th>Provincia</th>
                    <th>Proyectos</th>
                    <th>Obras</th>
                </tr>
            {% endblock %}

            {% block tableBody %}
                {% from 'macros/link.html.twig' import filterLink %}

                {% for province in paginator.getData() %}
                    <tr>
                        <td>{{ province.name }}</td>
                        <td>{{ province.projects }}</td>
                        <td>{{ province.buildings }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="3">No se encontraron provincias.</td>
                    </tr>
                {% endfor %}
            {% endblock %}

        {% endcomponent %}

    {% endblock %}

{% endcomponent %}
