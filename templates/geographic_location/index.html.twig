{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Ubicaciones geográficas{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Ubicaciones geográficas') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Ubicaciones'},
        ]) }}
    </div>

    <h2 class="mb-4">Gestión de ubicaciones geográficas</h2>

    {% component ManagePage %}
        {% block list %}
            {{ include('geographic_location/_list.html.twig') }}
        {% endblock %}
    {% endcomponent %}
{% endblock %}
