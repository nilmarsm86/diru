{% if app.request.query.get('modal') %}
    {{ component('CountryForm', {modal:app.request.query.get('modal'), cou: country, ajax: ajax}) }}
{% else %}

    {% component 'Card:Card' with {
        cssClass: 'mb-4',
        extra: '',
        headerCssClass: 'd-flex justify-content-between align-items-center',
        title: title
    } %}

        {% block header %}
            {{ include('partials/_card_header_options.html.twig', {
                goTo: 'app_country_new',
                label: 'Nuevo',
                title: 'Nuevo país',
                entity: country,
                state: 'form',
                showButton: (showButton is defined and showButton == true) ? true : false
            }) }}
        {% endblock %}

        {% block body %}
            {{ component('CountryForm', {modal:null, cou: country, ajax: ajax}) }}
        {% endblock %}

        {% block footer %}
            {{ include('partials/_card_footer.html.twig', {
                list: {href: 'app_country_index', label: 'Listado', title:'Volver al listado'},
                delete: {href: 'app_country_delete', label: 'Eliminar', title:'Eliminar país'},
                id: country.id,
                message: 'Está seguro que desea eliminar el país?',
                state: 'form',
                showButton: (showButton is defined and showButton == true) ? true : false,
                entity: country
            }) }}
        {% endblock %}

    {% endcomponent %}
{% endif %}
