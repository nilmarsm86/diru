{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: 'id="urbanization_estimate_detail_' ~ urbanization_estimate.id,
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: title,
} %}

    {% block header %}
        {{ include('partials/_card_header_options.html.twig', {
            goTo: 'app_urbanization_estimate_new',
            label: 'Nueva',
            title: 'Nueva estimación de urbanización',
            entity: urbanization_estimate,
            state: 'detail',
            showButton: (showButton is defined and showButton == true) ? true : false
        }) }}
    {% endblock %}

    {% block body %}
        <div class="table-responsive table mt-2" role="grid" aria-describedby="dataTable_info">
            <table class="table table-hover">
                <tbody>
                <tr>
                    <th>Concepto:</th>
                    <td>{{ urbanization_estimate.concept }}</td>
                </tr>
                </tbody>
            </table>
        </div>
    {% endblock %}

    {% block footer %}
        {{ include('partials/_card_footer.html.twig', {
            list: {href: 'app_urbanization_estimate_index', label: 'Listado', title:'Volver al listado'},
            edit: {href: 'app_urbanization_estimate_edit', label: 'Editar', title:'Editar estimado de urbanización'},
            delete: {href: 'app_urbanization_estimate_delete', label: 'Eliminar', title:'Eliminar estimado de urbanización'},
            id: urbanization_estimate.id,
            message: 'Está seguro que desea eliminar el estimado de urbanización?',
            state: 'detail',
            showButton: (showButton is defined and showButton == true) ? true : false
        }) }}
    {% endblock %}

{% endcomponent %}


