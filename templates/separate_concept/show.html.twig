{% extends 'base.html.twig' %}

{% block title %}SeparateConcept{% endblock %}

{% block body %}
    <h1>SeparateConcept</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ separate_concept.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ separate_concept.type }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ separate_concept.number }}</td>
            </tr>
            <tr>
                <th>Formula</th>
                <td>{{ separate_concept.formula }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ separate_concept.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_separate_concept_index') }}">back to list</a>

    <a href="{{ path('app_separate_concept_edit', {'id': separate_concept.id}) }}">edit</a>

    {{ include('separate_concept/_delete_form.html.twig') }}
{% endblock %}
