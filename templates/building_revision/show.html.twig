{% extends 'base.html.twig' %}

{% block title %}BuildingRevision{% endblock %}

{% block body %}
    <h1>BuildingRevision</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ building_revision.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ building_revision.createdAt ? building_revision.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ building_revision.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_building_revision_index') }}">back to list</a>

    <a href="{{ path('app_building_revision_edit', {'id': building_revision.id}) }}">edit</a>

    {{ include('building_revision/_delete_form.html.twig') }}
{% endblock %}
