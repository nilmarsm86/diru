{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Editar Municipio{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Municipios', 'stash:pin-place') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Municipios', href: path('app_municipality_index')},
            {label: 'Editar'},
        ]) }}
    </div>

    <h2 class="mb-4">Editar municipio</h2>

    {{ include('municipality/_form.html.twig', {'showButton': true, title: title}) }}
{% endblock %}
