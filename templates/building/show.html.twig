{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}

{% block title %}Obra: {{ building.name }}{% endblock %}
{% from 'macros/titles.html.twig' import h1 %}

{% block content %}
    <div class="row">
        {{ h1('Obras') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Obras', href: path('app_building_index')},
            {label: 'Detalle'},
        ]) }}
    </div>

    <h2 class="mb-4">Obra: {{ building.name }}</h2>

    {{ include('building/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
