{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Editar Persona{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Personas') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Personas', href: path('app_person_index')},
            {label: 'Editar'},
        ]) }}
    </div>

    <h2 class="mb-4">Editar representante</h2>

    {{ include('person/_form.html.twig', {showButton: true, title: title}) }}
{% endblock %}
