{% extends 'pdf_nomenclator.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div>
        <strong>Proyecto:</strong> {{ project.name }}
        {% if filter %}
            <br>
            <strong>Filtrado por:</strong> {{ filter }}
        {% endif %}
    </div>
    <br>
{#    {{ include('building/pdf/_list.html.twig') }}#}
    {% block data %}{% endblock %}
{% endblock %}
