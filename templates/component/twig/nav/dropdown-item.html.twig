{% from 'macros/link.html.twig' import activeLink %}

{% if icon is not defined %}
    {% set icon = null %}
{% endif %}

<li>
    <a class="dropdown-item d-flex gap-2 align-items-center {{ activeLink(base) }}"
       href="{% if link is not empty %}{{ path(link) }}{% endif %}"
    >
        {% if icon is not null %}
            {{ ux_icon(icon, {class:'bi'}) }}
        {% endif %}

        {{ label }}
    </a>
</li>
