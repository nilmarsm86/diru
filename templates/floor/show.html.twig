{% extends 'base.html.twig' %}

{% block title %}Floor{% endblock %}

{% block body %}
    <h1>Floor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ floor.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ floor.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_floor_index') }}">back to list</a>

    <a href="{{ path('app_floor_edit', {'id': floor.id}) }}">edit</a>

    {{ include('floor/_delete_form.html.twig') }}
{% endblock %}
