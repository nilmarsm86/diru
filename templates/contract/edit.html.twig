{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Editar Contrato{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Contratos') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Contratos', href: path('app_contract_index')},
            {label: 'Editar'},
        ]) }}
    </div>

    <h2 class="mb-4">Editar contrato</h2>

    {{ include('contract/_form.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
