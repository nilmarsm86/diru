<form method="post" action="{{ path('app_building_revision_delete', {'id': building_revision.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ building_revision.id) }}">
    <button class="btn">Delete</button>
</form>
