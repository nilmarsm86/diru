{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Zonas de ubicación', 'bi:pin-map') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Zonas de ubicación', href: path('app_location_zone_index')},
            {label: 'Reporte'}
        ]) }}
    </div>

    <h2 class="mb-4">{{ title }}</h2>

    {% component ManagePage %}
        {% block list %}
            {{ include('location_zone/report/'~list~'.html.twig') }}
        {% endblock %}
    {% endcomponent %}
{% endblock %}
