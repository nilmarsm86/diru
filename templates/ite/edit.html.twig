{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Editar ITE{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('ITE') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'ITE', href: path('app_ite_index')},
            {label: 'Editar'},
        ]) }}
    </div>

    <h2 class="mb-4">Editar ITE</h2>

    {{ include('ite/_form.html.twig', {showButton: true, title: title}) }}
{% endblock %}
