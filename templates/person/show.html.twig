{% extends 'content.html.twig' %}

{% block title %}Person{% endblock %}

{% block content %}
    <h1>Persona</h1>

    <table class="table">
        <tbody>
        {#            <tr> #}
        {#                <th>Id</th> #}
        {#                <td>{{ person.id }}</td> #}
        {#            </tr> #}
        <tr>
            <th>Nombre</th>
            <td>{{ person.name }}</td>
        </tr>
        <tr>
            <th>Carne</th>
            <td>{{ person.identificationNumber }}</td>
        </tr>
        <tr>
            <th>Pasaporte</th>
            <td>{{ person.passport }}</td>
        </tr>

        </tbody>
    </table>

    <a class="btn btn-sm btn-primary" href="{{ path('app_person_index') }}">Listado</a>

    <a class="btn btn-sm btn-primary" href="{{ path('app_person_edit', {'id': person.id}) }}">Modificar</a>

    {{ include('person/_delete_form.html.twig') }}
{% endblock %}
