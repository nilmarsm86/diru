{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Nueva Ubicación{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Ubicaciones geográficas', 'bi:pin-map-fill') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Ubicación', href: path('app_geographic_location_index')},
            {label: 'Nueva'},
        ]) }}
    </div>

    <h2 class="mb-4">Agregar Ubicación geográfica</h2>

    {{ include('geographic_location/_form.html.twig', {showButton: true, title: title}) }}
{% endblock %}
