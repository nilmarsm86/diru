{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Datos de los terrenos{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Datos del terreno') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Terrenos'},
        ]) }}
    </div>

    <h2 class="mb-4">Datos de los terrenos</h2>

    {% component ManagePage %}
        {% block list %}
            {{ include('land/_list.html.twig') }}
        {% endblock %}
    {% endcomponent %}
{% endblock %}
