{% extends 'location_zone/pdf/pdf.html.twig' %}

{% block columns %}
    <tr>
        {{ th(0, 'column', 'Nombre') }}
        {{ th(1, 'column', 'Clientes') }}
        {{ th(2, 'column', 'Constructora') }}
        {{ th(3, 'column', 'Cliente y Constructora') }}
        {{ th(4, 'column', 'Proyectista') }}
        {{ th(5, 'column', 'Total') }}
    </tr>
{% endblock %}

{% block data %}
    <tr>
        {{ td(0, 'column', organization.name) }}
        {{ td(1, 'column', organization.client) }}
        {{ td(2, 'column', organization.constructor) }}
        {{ td(3, 'column', organization.client_constructor) }}
        {{ td(4, 'column', organization.draftman) }}
        {{ td(5, 'column', organization.total) }}
    </tr>
{% endblock %}
