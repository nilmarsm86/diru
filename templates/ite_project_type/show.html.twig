{% extends 'base.html.twig' %}

{% block title %}IteProjectType{% endblock %}

{% block body %}
    <h1>IteProjectType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ite_project_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ite_project_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ite_project_type_index') }}">back to list</a>

    <a href="{{ path('app_ite_project_type_edit', {'id': ite_project_type.id}) }}">edit</a>

    {{ include('ite_project_type/_delete_form.html.twig') }}
{% endblock %}
