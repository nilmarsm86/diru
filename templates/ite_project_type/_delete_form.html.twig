<form method="post" action="{{ path('app_ite_project_type_delete', {'id': ite_project_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ite_project_type.id) }}">
    <button class="btn">Delete</button>
</form>
