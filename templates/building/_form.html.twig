{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: '',
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: title
} %}

    {% block header %}
        {% from 'macros/buttons.html.twig' import tableActionButton2,tableActionButton3, tableHeaderButton, tableHeaderButtonDropdown %}

        {#        {{ include('partials/_card_header_options.html.twig', { #}
        {#            goTo: 'app_building_new', #}
        {#            label: 'Nueva', #}
        {#            title: 'Nueva obra', #}
        {#            entity: building, #}
        {#            state: 'form', #}
        {#            showButton: (showButton is defined and showButton == true) ? true : false #}
        {#        }) }} #}

        {% if building.id is not null %}
            {{ tableActionButton3(path('app_land_new'), 'bi:plus-lg', 'Datos genrales del terreno', 'Datos del terreno', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_land_new')})) }}
        {% else %}
            {{ tableActionButton3(path('app_land_edit', {id:0}), 'bi:plus-lg', 'Datos genrales del terreno', 'Datos del terreno', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_land_edit', {id:0})})) }}
        {% endif %}

        {#    {{ tableActionButton3(path('app_corporate_entity_new'), 'bi:plus-lg', 'Informe de la obra', 'Informe de la obra', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_corporate_entity_new')})) }} #}
        {#    {{ tableActionButton3(path('app_corporate_entity_new'), 'bi:plus-lg', 'Gestionar inmueble construido', 'Gestionar inmueble construido', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_corporate_entity_new')})) }} #}
        {#    {{ tableActionButton3(path('app_corporate_entity_new'), 'bi:plus-lg', 'Gestgionar inmueble nuevo', 'Gestgionar inmueble nuevo', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_corporate_entity_new')})) }} #}
        {#    {{ tableActionButton3(path('app_corporate_entity_new'), 'bi:plus-lg', 'Regulaciones urbanas', 'Regulaciones urbanas', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_corporate_entity_new')})) }} #}
        {#    {{ tableActionButton3(path('app_corporate_entity_new'), 'bi:plus-lg', 'Analisis del valor de constrccion y montaje', 'Analisis del valor de constrccion y montaje', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_corporate_entity_new')})) }} #}
    {% endblock %}

    {% block body %}
        {{ component('BuildingForm', {modal:null, bui: building, ajax: ajax}) }}
    {% endblock %}

    {% block footer %}
        {{ include('partials/_card_footer.html.twig', {
            list: {href: 'app_project_edit', data:{id: building.project.id}, label: 'Proyecto', title:'Volver al proyecto'},
            delete: null,
            id: building.id,
            message: '',
            state: 'form',
            showButton: (showButton is defined and showButton == true) ? true : false,
            entity: building
        }) }}
    {% endblock %}

{% endcomponent %}

{{ include('partials/modal/_modal_load.html.twig') }}
