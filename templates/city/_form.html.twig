{% if app.request.query.get('modal') %}
    {{ component('CityForm', {modal:app.request.query.get('modal'), cit: city, ajax: ajax}) }}
{% else %}
    {% component 'Card:Card' with {
        cssClass: 'mb-4',
        extra: '',
        headerCssClass: 'd-flex justify-content-between align-items-center',
        title: title
    } %}

        {% block header %}
            {{ include('partials/_card_header_options.html.twig', {
                goTo: 'app_city_new',
                label: 'Nueva',
                title: 'Nueva ciudad',
                entity: city,
                state: 'form',
                showButton: (showButton is defined and showButton == true) ? true : false
            }) }}
        {% endblock %}

        {% block body %}
            {{ component('CityForm', {modal:null, cit: city, ajax: ajax}) }}
        {% endblock %}

        {% block footer %}
            {{ include('partials/_card_footer.html.twig', {
                list: {href: 'app_city_index', label: 'Listado', title:'Volver al listado'},
                delete: {href: 'app_city_delete', label: 'Eliminar', title:'Eliminar ciudad'},
                id: city.id,
                message: 'Está seguro que desea eliminar la ciudad?',
                state: 'form',
                showButton: (showButton is defined and showButton == true) ? true : false,
                entity: city
            }) }}
        {% endblock %}

    {% endcomponent %}
{% endif %}

{{ include('partials/modal/_modal_load.html.twig') }}
