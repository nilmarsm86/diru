{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Sub sistemas{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Sub sistemas') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Sub sistemas'},
        ]) }}
    </div>

    <h2 class="mb-4">
        Subsistemas de:
    </h2>

    {{ include('sub_system/_amount_local_technical_status.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
