{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}

{% block title %}Ubicación geográfica: {{ geographic_location.name }}{% endblock %}
{% from 'macros/titles.html.twig' import h1 %}

{% block content %}
    <div class="row">
        {{ h1('Ubicación geográfica', 'bi:pin-map-fill') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Ubicación', href: path('app_geographic_location_index')},
            {label: 'Detalle'},
        ]) }}
    </div>

    <h2 class="mb-4">Ubicación geográfica: {{ geographic_location.name }}</h2>

    {{ include('geographic_location/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
