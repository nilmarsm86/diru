{% extends 'base.html.twig' %}

{% block title %}IteSource{% endblock %}

{% block body %}
    <h1>IteSource</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ite_source.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ite_source.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ite_source_index') }}">back to list</a>

    <a href="{{ path('app_ite_source_edit', {'id': ite_source.id}) }}">edit</a>

    {{ include('ite_source/_delete_form.html.twig') }}
{% endblock %}
