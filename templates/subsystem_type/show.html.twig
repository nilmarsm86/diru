{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Tipo: {{ subsystem_type.name }}{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Tipos') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Tipos', href: path('app_subsystem_type_index')},
            {label: 'Detalle'},
        ]) }}
    </div>

    <h2 class="mb-4">Tipo: {{ subsystem_type.name }}</h2>

    {{ include('subsystem_type/_detail.html.twig', {'showButton': true, 'title': title}) }}
{% endblock %}
