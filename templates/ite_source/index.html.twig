{% extends 'base.html.twig' %}

{% block title %}IteSource index{% endblock %}

{% block body %}
    <h1>IteSource index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ite_source in ite_sources %}
            <tr>
                <td>{{ ite_source.id }}</td>
                <td>{{ ite_source.name }}</td>
                <td>
                    <a href="{{ path('app_ite_source_show', {'id': ite_source.id}) }}">show</a>
                    <a href="{{ path('app_ite_source_edit', {'id': ite_source.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ite_source_new') }}">Create new</a>
{% endblock %}
