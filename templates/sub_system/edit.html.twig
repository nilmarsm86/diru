{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Editar Subsistema{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Subsistemas', 'ix:building-block-filled') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Subsistemas', href: path('app_sub_system_index', 'floor':floor.id)},
            {label: 'Editar'},
        ]) }}
    </div>

    <h2 class="mb-4">Editar subsistema de la planta: {{ floor.name }}</h2>

    {{ include('sub_system/_form.html.twig', {'showButton': true, title: title}) }}
{% endblock %}
