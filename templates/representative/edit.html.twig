{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Editar Representante{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Representantes') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Representantes', href: path('app_representative_index')},
            {label: 'Editar'},
        ]) }}
    </div>

    <h2 class="mb-4">Editar representante</h2>

    {{ include('representative/_form.html.twig', {showButton: true, title: title}) }}
{% endblock %}
