{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: '',
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: 'Listado de tipos',
} %}
    {% block header %}
        {% from 'macros/buttons.html.twig' import tableActionButton2,tableActionButton3 %}

        <div>
            {{ tableActionButton3(path('app_subsystem_type_new'), 'bi:plus-lg', 'Nuevo', 'Nuevo tipo', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_subsystem_type_new')})) }}
        </div>
    {% endblock %}

    {% block body %}
        {% component 'Table:Table' with {
            paginator: paginator,
        } %}

            {% block tableHead %}
                <tr>
{#                    <th style="width: 1em"></th>#}
                    <th>Nombre</th>
                    <th></th>
                </tr>
            {% endblock %}

            {% block tableBody %}
                {% from 'macros/buttons.html.twig' import tableActionButton, deleteFormButton, deleteFormButton2, tableActionButton2, tableActionButton3 %}

                {% for subsystemType in paginator.getData() %}
                    <tr {{ stimulusController('delete-form-container') }}>
{#                        <td style="width: 1em">#}
{#                            <input#}
{#                                    class="form-check-input"#}
{#                                    type="checkbox"#}
{#                                    id="{{ province.id }}"#}
{#                            >#}
{#                        </td>#}
                        <td>{{ subsystemType.name }}</td>
                        <td class="text-end">
                            {{ tableActionButton3(path('app_subsystem_type_show', {'id': subsystemType.id}), 'bi:eye', '', 'Detalle del tipo', stimulusAction('twig/table/table', 'selectElement:prevent', null, {url:path('app_subsystem_type_show', {'id': subsystemType.id})})) }}
                            {{ tableActionButton3(path('app_subsystem_type_edit', {'id': subsystemType.id}), 'fa:edit', '', 'Editar tipo', stimulusAction('twig/table/table', 'selectElement:prevent', null, {url:path('app_subsystem_type_edit', {'id': subsystemType.id})})) }}

                            {{ component('DeleteForm', {
                                path: path('app_subsystem_type_delete', {'id': subsystemType.id}),
                                confirm: 'Está seguro que desea eliminar el tipo?',
                                token: 'delete' ~ subsystemType.id,
                                title: 'Eliminar tiopo',
                                icon: 'bi:trash'
                            }) }}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="2">No se encontraron tipos de subsistema.</td>
                    </tr>
                {% endfor %}
            {% endblock %}

        {% endcomponent %}

    {% endblock %}

{% endcomponent %}
