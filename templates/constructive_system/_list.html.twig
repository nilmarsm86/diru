{% component 'Card:Card' with {
    cssClass: 'mb-4',
    extra: '',
    headerCssClass: 'd-flex justify-content-between align-items-center',
    title: 'Listado de sistemas constructivos',
} %}
    {% block header %}
        {% from 'macros/buttons.html.twig' import tableActionButton2,tableActionButton3 %}

        <div>
            {{ tableActionButton3(path('app_constructive_system_new'), 'bi:plus-lg', 'Nuevo', 'Nuevo sistema constructivo', stimulusAction('twig/card/card', 'newElement:prevent', null, {url:path('app_constructive_system_new')})) }}
        </div>
    {% endblock %}

    {% block body %}
        {% component 'Table:Table' with {
            paginator: paginator,
        } %}

            {% block tableHead %}
                <tr>
                    <th>Nombre</th>
                    <th></th>
                </tr>
            {% endblock %}

            {% block tableBody %}
                {% from 'macros/buttons.html.twig' import tableActionButton, deleteFormButton, deleteFormButton2, tableActionButton2, tableActionButton3 %}

                {% for constructive_system in paginator.getData() %}
                    <tr {{ stimulusController('delete-form-container') }}>
                        <td>{{ constructive_system.name }}</td>
                        <td class="text-end">
                            {{ tableActionButton3(path('app_constructive_system_edit', {'id': constructive_system.id}), 'fa:edit', '', 'Editar sistema constructivo', stimulusAction('twig/table/table', 'selectElement:prevent', null, {url:path('app_constructive_system_edit', {'id': constructive_system.id})}), 'btn-link') }}

                            {{ component('DeleteForm', {
                                path: path('app_constructive_system_delete', {'id': constructive_system.id}),
                                confirm: 'Está seguro que desea eliminar el sistema constructivo?',
                                token: 'delete' ~ constructive_system.id,
                                title: 'Eliminar sistema constructivo',
                                icon: 'bi:trash',
                                cssClass: 'btn-link text-danger'
                            }) }}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="2">No se encontraron sistemas constructivos.</td>
                    </tr>
                {% endfor %}
            {% endblock %}

        {% endcomponent %}

    {% endblock %}

{% endcomponent %}
