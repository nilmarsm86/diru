<form method="post" action="{{ path('app_location_zone_delete', {'id': location_zone.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location_zone.id) }}">
    <button class="btn">Delete</button>
</form>
