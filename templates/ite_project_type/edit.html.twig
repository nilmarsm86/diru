{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Editar tipo de proyecto{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Tipo de proyecto', 'oi:project') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Tipos', href: path('app_ite_project_type_index')},
            {label: 'Editar'},
        ]) }}
    </div>

    <h2 class="mb-4">Editar tipo de proyecto</h2>

    {{ include('ite_project_type/_form.html.twig', {showButton: true, title: title}) }}
{% endblock %}
