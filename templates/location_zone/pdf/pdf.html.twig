{% extends 'pdf_nomenclator.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
    <div>
        <strong>Ubicaciones:</strong> Zonas de localización
        {% if filter %}
            <br>
            <strong>Filtrado por:</strong> {{ filter }}
        {% endif %}
    </div>
    <br>
    <table>
        {% block columns %}{% endblock %}
        {% for location_zone in paginator.getData() %}
            {% block data %}{% endblock %}
        {% else %}
            <tr>
                <td colspan="{{ colspan|default(1) }}">No se encontraron zonas de localización.</td>
            </tr>
        {% endfor %}
    </table>
{% endblock %}
