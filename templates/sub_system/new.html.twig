{% extends 'content.html.twig' %}

{% from 'macros/breadcrumb.html.twig' import breadcrumb %}
{% from 'macros/titles.html.twig' import h1 %}

{% block title %}Nuevo Sub sistema{% endblock %}

{% block content %}
    <div class="row">
        {{ h1('Sub sistema', 'ix:building-block-filled') }}

        {{ breadcrumb([
            {label: 'Inicio', href: path('app_home')},
            {label: 'Sub sistemas', href: path('app_sub_system_index', {'floor':floor.id})},
            {label: 'Nuevo'},
        ]) }}
    </div>

    <h2 class="mb-4">Agregar sub sistema: {{ floor.name }}</h2>

    {{ include('sub_system/_form.html.twig', {'showButton': true, title: title}) }}
{% endblock %}
